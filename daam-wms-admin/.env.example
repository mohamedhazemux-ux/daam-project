# Browser-visible URL of the API. Leave as /api when using the Vite proxy below.
VITE_API_BASE_URL=/api

# Local development only: API server that receives /api requests from Vite.
# VITE_API_PROXY_TARGET=http://localhost:3000
